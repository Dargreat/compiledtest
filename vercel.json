{
  "version": 2,
   "buildCommand": "npm install -D tailwindcss postcss autoprefixer && npx tailwindcss init -p && npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "functions": {
    "pages/api/**": {
      "maxDuration": 30
    }
  },
  "env": {
    "PRISMA_GENERATE_SKIP_AUTOINSTALL": "true"
  }
}
